import { ProcessContextPayload } from '../../../internal/types';
/***
 * Converts text strings into numerical vector representations (embeddings)
 * that can be used for similarity search, clustering, or ML applications.
 */
export interface TextEmbedderInterface {
    /**
     * Embed a single text string
     *
     * @param text - The text to embed
     * @returns Promise that resolves to Float32Array containing the embedding vector
     *
     * @example
     * const embedding = await embedder.embed("Hello world");
     * console.log(embedding);  // Float32Array [0.1, -0.2, 0.5, ...]
     * console.log(embedding.length);  // e.g., 768 dimensions
     */
    embed(text: string): Promise<Float32Array>;
    /**
     * Embed a batch of text strings
     *
     * @param texts - Array of texts to embed
     * @returns Promise that resolves to array of Float32Arrays, one per input text
     *
     * @example
     * const embeddings = await embedder.embedBatch([
     *   "First text",
     *   "Second text",
     *   "Third text"
     * ]);
     * console.log(embeddings.length);  // 3
     * console.log(embeddings[0]);  // Float32Array [...]
     */
    embedBatch(texts: string[]): Promise<Float32Array[]>;
}
export declare class TextEmbedderInterfaceWrapper implements TextEmbedderInterface {
    private embedder;
    constructor(componentRegistry: ProcessContextPayload['componentRegistry'], componentId: string);
    embed(text: string): Promise<Float32Array>;
    embedBatch(texts: string[]): Promise<Float32Array[]>;
}
