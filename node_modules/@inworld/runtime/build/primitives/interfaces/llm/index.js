"use strict";
var __esDecorate = (this && this.__esDecorate) || function (ctor, descriptorIn, decorators, contextIn, initializers, extraInitializers) {
    function accept(f) { if (f !== void 0 && typeof f !== "function") throw new TypeError("Function expected"); return f; }
    var kind = contextIn.kind, key = kind === "getter" ? "get" : kind === "setter" ? "set" : "value";
    var target = !descriptorIn && ctor ? contextIn["static"] ? ctor : ctor.prototype : null;
    var descriptor = descriptorIn || (target ? Object.getOwnPropertyDescriptor(target, contextIn.name) : {});
    var _, done = false;
    for (var i = decorators.length - 1; i >= 0; i--) {
        var context = {};
        for (var p in contextIn) context[p] = p === "access" ? {} : contextIn[p];
        for (var p in contextIn.access) context.access[p] = contextIn.access[p];
        context.addInitializer = function (f) { if (done) throw new TypeError("Cannot add initializers after decoration has completed"); extraInitializers.push(accept(f || null)); };
        var result = (0, decorators[i])(kind === "accessor" ? { get: descriptor.get, set: descriptor.set } : descriptor[key], context);
        if (kind === "accessor") {
            if (result === void 0) continue;
            if (result === null || typeof result !== "object") throw new TypeError("Object expected");
            if (_ = accept(result.get)) descriptor.get = _;
            if (_ = accept(result.set)) descriptor.set = _;
            if (_ = accept(result.init)) initializers.unshift(_);
        }
        else if (_ = accept(result)) {
            if (kind === "field") initializers.unshift(_);
            else descriptor[key] = _;
        }
    }
    if (target) Object.defineProperty(target, contextIn.name, descriptor);
    done = true;
};
var __runInitializers = (this && this.__runInitializers) || function (thisArg, initializers, value) {
    var useValue = arguments.length > 2;
    for (var i = 0; i < initializers.length; i++) {
        value = useValue ? initializers[i].call(thisArg, value) : initializers[i].call(thisArg);
    }
    return useValue ? value : void 0;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.LLMInterfaceWrapper = void 0;
const graphs_1 = require("../../../common/api/graphs");
const llm_chat_request_1 = require("../../../common/data_types/api/llm_chat_request");
const stream_1 = require("../../../common/stream");
const internal_1 = __importDefault(require("../../../internal/decorators/internal"));
const inworld_1 = require("../../../internal/error/inworld");
let LLMInterfaceWrapper = (() => {
    var _a;
    let _llm_decorators;
    let _llm_initializers = [];
    let _llm_extraInitializers = [];
    return _a = class LLMInterfaceWrapper {
            constructor(componentRegistry, componentId) {
                this.llm = __runInitializers(this, _llm_initializers, void 0);
                __runInitializers(this, _llm_extraInitializers);
                const llm = componentRegistry.getComponent(componentId);
                if (!llm || !('generateContent' in llm)) {
                    throw new inworld_1.InworldError(`LLMInterface with id ${componentId} is not specified.`);
                }
                this.llm = llm;
            }
            async generateContent(input, tools, toolChoice, responseFormat) {
                if (typeof input === 'string') {
                    const stream = await this.llm.generateContent(input);
                    return stream_1.ContentStream.fromExternal(stream);
                }
                else if (input instanceof llm_chat_request_1.LLMChatRequest) {
                    const stream = await this.llm.generateContentMessages(input.toProto());
                    return stream_1.ContentStream.fromExternal(stream);
                }
                return this.generateContentMessages(input, tools, toolChoice, responseFormat);
            }
            async generateContentMessages(messages, tools, toolChoice, responseFormat) {
                const stream = await this.llm.generateContentMessages(new llm_chat_request_1.LLMChatRequest(graphs_1.LLMChatRequestSchema.parse({
                    messages,
                    tools,
                    toolChoice,
                    responseFormat,
                })).toProto());
                return stream_1.ContentStream.fromExternal(stream);
            }
        },
        (() => {
            const _metadata = typeof Symbol === "function" && Symbol.metadata ? Object.create(null) : void 0;
            _llm_decorators = [internal_1.default];
            __esDecorate(null, null, _llm_decorators, { kind: "field", name: "llm", static: false, private: false, access: { has: obj => "llm" in obj, get: obj => obj.llm, set: (obj, value) => { obj.llm = value; } }, metadata: _metadata }, _llm_initializers, _llm_extraInitializers);
            if (_metadata) Object.defineProperty(_a, Symbol.metadata, { enumerable: true, configurable: true, writable: true, value: _metadata });
        })(),
        _a;
})();
exports.LLMInterfaceWrapper = LLMInterfaceWrapper;
