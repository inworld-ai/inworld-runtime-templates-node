/**
 * Types specific to MCP primitives (MCPClient).
 */
import type { ToolCallResult } from '../../common/api/graphs';
import type { ToolList } from '../../common/api/library';
import { Tool, ToolCall } from '../types';
/**
 * MCP Client types
 */
export interface MCPClientCreationConfig {
    sessionConfig: {
        transport: 'http' | 'stdio';
        endpoint: string;
        httpConfig?: {
            apiKey: string;
            defaultTimeout?: string;
        };
        stdioConfig?: {
            env?: Record<string, string>;
        };
    };
}
export { Tool, ToolCall, ToolCallResult, ToolList };
