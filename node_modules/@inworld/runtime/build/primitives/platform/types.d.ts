/**
 * Types specific to platform primitives (DeviceRegistry).
 */
import { Device, DeviceDescriptor, DeviceInfo, DeviceType } from '../types';
export { Device, DeviceDescriptor, DeviceInfo, DeviceType };
