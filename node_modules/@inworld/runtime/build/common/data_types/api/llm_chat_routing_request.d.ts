import { PbBaseType } from '../../../internal/pb';
import { TaggedType } from '../../../internal/types';
import { LLMChatRoutingRequest as LLMChatRoutingRequestInterface } from '../../api/graphs';
import { AbstractApiDataType } from './abstract_api_data_type';
/**
 * Represents a complete chat routing request for LLM processing with
 * intelligent fallback.
 *
 * This class stores messages, model configuration, and routing configuration
 * for LLM requests, enabling automatic fallback to alternative models when the
 * primary model fails.
 */
export declare class LLMChatRoutingRequest extends AbstractApiDataType implements LLMChatRoutingRequestInterface {
    /**
     * The messages to be sent to the LLM.
     */
    readonly messages: LLMChatRoutingRequestInterface['messages'];
    /**
     * The primary model to use for the request.
     */
    readonly modelId?: LLMChatRoutingRequestInterface['modelId'];
    /**
     * The routing configuration with fallback models.
     */
    readonly modelSelection?: LLMChatRoutingRequestInterface['modelSelection'];
    /**
     * Optional prompt variables for Jinja template rendering.
     * Used to render message templates in the routing strategy.
     */
    readonly promptVariables?: LLMChatRoutingRequestInterface['promptVariables'];
    /**
     * The response format to use for the LLM.
     */
    readonly responseFormat?: LLMChatRoutingRequestInterface['responseFormat'];
    /**
     * The routing strategy configuration.
     */
    readonly strategy?: LLMChatRoutingRequestInterface['strategy'];
    /**
     * Whether to stream the response.
     */
    readonly stream?: boolean;
    /**
     * The text generation configuration to use for the LLM.
     */
    readonly textGenerationConfig?: LLMChatRoutingRequestInterface['textGenerationConfig'];
    /**
     * The tool choice configuration to use for the LLM.
     */
    readonly toolChoice?: LLMChatRoutingRequestInterface['toolChoice'];
    /**
     * The tools available to the LLM.
     */
    readonly tools?: LLMChatRoutingRequestInterface['tools'];
    /**
     * Creates a new LLMChatRoutingRequest instance.
     * @param {LLMChatRoutingRequestInterface} llmChatRoutingRequest - The chat
     *     routing request to wrap.
     */
    constructor(llmChatRoutingRequest: LLMChatRoutingRequestInterface);
    /** @internal */
    toTaggedValue(): TaggedType;
    /** @internal */
    toProto(): PbBaseType;
}
