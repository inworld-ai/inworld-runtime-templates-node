import { PbBaseType } from '../../../internal/pb';
import { TaggedType } from '../../../internal/types';
import { ToolCallResponse as ToolCallResponseInterface } from '../../api/graphs';
import { AbstractApiDataType } from './abstract_api_data_type';
/**
 * Represents the result of a tool call execution.
 *
 * This class encapsulates the result from executing a tool call,
 * including the tool call ID and the execution result.
 */
export declare class ToolCallResponse extends AbstractApiDataType implements ToolCallResponseInterface {
    /**
     * The tool call execution results.
     */
    readonly toolCallResults: ToolCallResponseInterface['toolCallResults'];
    /**
     * Creates a new ToolCallResponse instance.
     * @param input
     */
    constructor(input: ToolCallResponseInterface);
    /** @internal */
    toTaggedValue(): TaggedType;
    /** @internal */
    toProto(): PbBaseType;
}
