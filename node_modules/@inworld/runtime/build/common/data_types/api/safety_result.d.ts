import { PbBaseType } from '../../../internal/pb';
import { TaggedType } from '../../../internal/types';
import { AbstractApiDataType } from './abstract_api_data_type';
/**
 * Interface representing a safety evaluation result.
 */
export interface SafetyResultInterface {
    /** Whether the content is considered safe */
    isSafe: boolean;
    /** The text that was evaluated for safety */
    text: string;
}
/**
 * Represents the result of safety evaluation operations.
 *
 * This class encapsulates the result from safety checking, containing
 * information about whether content is safe and what triggered any violations.
 */
export declare class SafetyResult extends AbstractApiDataType implements SafetyResultInterface {
    /**
     * Whether the content is considered safe.
     */
    readonly isSafe: boolean;
    /**
     * The text that was evaluated for safety.
     */
    readonly text: string;
    /**
     * Creates a new SafetyResult instance.
     * @param result The safety evaluation result data.
     */
    constructor(result: SafetyResultInterface);
    /** @internal */
    toTaggedValue(): TaggedType;
    /** @internal */
    toProto(): PbBaseType;
}
