import { PbBaseType } from '../../../internal/pb';
import { TaggedType } from '../../../internal/types';
import { AudioChunkInterface } from '../common';
import { AbstractApiDataType } from './abstract_api_data_type';
/**
 * Representation of audio waveform data, used to pass audio data into the Inworld Runtime streams
 * and to inworld runtime graph.
 */
export declare class Audio extends AbstractApiDataType implements AudioChunkInterface {
    /**
     * PCM Float32 waveform samples. Values are typically normalized to the range [-1.0, 1.0].
     */
    readonly data: number[];
    /**
     * Sampling rate in hertz (Hz).
     */
    readonly sampleRate: number;
    /**
     * Creates an `Audio` object.
     *
     * @param chunk - Low-level audio chunk containing waveform data and sample rate.
     */
    constructor(chunk: AudioChunkInterface);
    /** @internal */
    toTaggedValue(): TaggedType;
    /** @internal */
    toProto(): PbBaseType;
}
