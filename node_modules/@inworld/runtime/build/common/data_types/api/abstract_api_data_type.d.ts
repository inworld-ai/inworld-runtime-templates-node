import { PbBaseType } from '../../../internal/pb';
import { TaggedType } from '../../../internal/types';
/**
 * Abstract base class for all API-level data types.
 */
export declare abstract class AbstractApiDataType {
    /**
     * @internal
     */
    abstract toTaggedValue(): TaggedType;
    /**
     * @internal
     */
    abstract toProto(): PbBaseType;
    /**
     * Static method to check if a value is an instance of AbstractApiDataType.
     * @param value - The value to check
     * @returns True if the value is an API data type instance
     */
    static isApiDataType(value: any): value is AbstractApiDataType;
}
