import { PbBaseType } from '../../../internal/pb';
import { TaggedType } from '../../../internal/types';
import { KnowledgeRecords as KnowledgeRecordsInterface } from '../../api/graphs';
import { AbstractApiDataType } from './abstract_api_data_type';
/**
 * Represents a collection of knowledge records as strings.
 *
 * This class stores multiple knowledge records that can be used for
 * information retrieval or other knowledge-based operations.
 */
export declare class KnowledgeRecords extends AbstractApiDataType implements KnowledgeRecordsInterface {
    /**
     * The stored knowledge records.
     */
    readonly records: string[];
    /**
     * Creates a new KnowledgeRecords instance.
     * @param input The list of knowledge records to store.
     */
    constructor(input: KnowledgeRecordsInterface);
    /** @internal */
    toTaggedValue(): TaggedType;
    /** @internal */
    toProto(): PbBaseType;
}
