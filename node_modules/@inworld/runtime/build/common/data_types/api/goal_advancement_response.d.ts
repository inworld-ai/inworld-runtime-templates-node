import { PbBaseType } from '../../../internal/pb';
import { TaggedType } from '../../../internal/types';
import { GoalAdvancementResponse as GoalAdvancementResponseInterface } from '../common';
import { AbstractApiDataType } from './abstract_api_data_type';
/**
 * Represents the result of goal state advancement.
 *
 * Tracks goals that were activated, completed, and currently active during
 * a graph execution cycle.
 */
export declare class GoalAdvancementResponse extends AbstractApiDataType implements GoalAdvancementResponseInterface {
    /**
     * Goals that have been newly activated.
     */
    readonly activatedGoals: string[];
    /**
     * Goals that have been successfully completed.
     */
    readonly completedGoals: string[];
    /**
     * Goals that are currently being pursued.
     */
    readonly currentGoals: string[];
    readonly beliefState: any;
    /**
     * Creates a new GoalAdvancementResponse instance.
     * @param goalAdvancement The goal advancement data to create the instance from.
     */
    constructor(goalAdvancement: GoalAdvancementResponseInterface);
    /** @internal */
    toTaggedValue(): TaggedType;
    /** @internal */
    toProto(): PbBaseType;
}
