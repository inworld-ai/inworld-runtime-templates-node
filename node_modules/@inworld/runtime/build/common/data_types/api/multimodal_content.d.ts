import { PbBaseType } from '../../../internal/pb';
import { TaggedType } from '../../../internal/types';
import { AbstractApiDataType } from './abstract_api_data_type';
import { Audio } from './audio';
/**
 * Interface for MultimodalContent data, which can contain either text or audio.
 */
export interface MultimodalContentInterface {
    text?: string;
    audio?: Audio;
}
/**
 * Representation of multimodal content data, used to pass either text or audio data
 * into the Inworld Runtime streams and to inworld runtime graph.
 * This is typically used for C++ native graphs that expect MultimodalContent protobuf messages.
 */
export declare class MultimodalContent extends AbstractApiDataType {
    /**
     * Text content (optional).
     */
    readonly text?: string;
    /**
     * Audio content (optional).
     */
    readonly audio?: Audio;
    /**
     * Creates a `MultimodalContent` object.
     *
     * @param content - Content containing either text or audio (or both).
     */
    constructor(content: MultimodalContentInterface);
    /** @internal */
    toTaggedValue(): TaggedType;
    /** @internal */
    toProto(): PbBaseType;
}
