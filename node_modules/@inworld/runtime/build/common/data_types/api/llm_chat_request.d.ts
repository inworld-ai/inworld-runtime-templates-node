import { PbBaseType } from '../../../internal/pb';
import { TaggedType } from '../../../internal/types';
import { LLMChatRequest as LLMChatRequestInterface, Message, ResponseFormat, Tool, ToolChoice } from '../../api/graphs';
import { TextGenerationConfig } from '../../api/library';
import { AbstractApiDataType } from './abstract_api_data_type';
/**
 * Represents a complete chat request for LLM processing.
 *
 * This class stores both messages and available tools for LLM requests,
 * using the same format as the LLM interface to support advanced features
 * like tool calls.
 */
export declare class LLMChatRequest extends AbstractApiDataType implements LLMChatRequestInterface {
    /**
     * The messages to be sent to the LLM.
     */
    readonly messages: Message[];
    /**
     * The tools available to the LLM.
     */
    readonly tools?: Tool[];
    /**
     * The response format to use for the LLM.
     */
    readonly responseFormat?: ResponseFormat;
    /**
     * The tool choice configuration to use for the LLM.
     */
    readonly toolChoice?: ToolChoice;
    /**
     * The text generation configuration to use for the LLM.
     */
    readonly textGenerationConfig?: TextGenerationConfig;
    /**
     * Creates a new LLMChatRequest instance.
     * @param {LLMChatRequestInterface} llmChatRequest - The chat request to wrap.
     */
    constructor(llmChatRequest: LLMChatRequestInterface);
    /** @internal */
    private convertMessageToTagged;
    /** @internal */
    private convertToolChoiceToTagged;
    /** @internal */
    toTaggedValue(): TaggedType;
    /** @internal */
    toProto(): PbBaseType;
}
