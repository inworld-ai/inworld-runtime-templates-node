import { PbBaseType } from '../../../internal/pb';
import { TaggedType } from '../../../internal/types';
import { IntentMatchInterface } from '../common';
import { AbstractApiDataType } from './abstract_api_data_type';
/**
 * Represents the result of intent matching containing the matched
 * intents with scores.
 *
 * This class stores the results of intent matching operations, including
 * a collection of matched intents with their confidence scores.
 */
export declare class MatchedIntents extends AbstractApiDataType {
    /**
     * The matched intents along with their confidence scores.
     */
    readonly intents: IntentMatchInterface[];
    /**
     * Creates a new MatchedIntents instance.
     * @param intents The list of matched intents with scores.
     */
    constructor(intents: IntentMatchInterface[]);
    /** @internal */
    toTaggedValue(): TaggedType;
    /** @internal */
    toProto(): PbBaseType;
}
