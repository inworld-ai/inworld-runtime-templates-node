import { RuntimeInputStream, StreamingTaggedTypeBase } from '../../internal/types';
import { TextStreamIterationResult } from '../data_types/common';
import { AbstractStreamType } from './abstract_stream_type';
import { Streamable } from './types';
/**
 * Streaming interface for text content from graph nodes.
 *
 * Provides asynchronous streaming for text data with transformation capabilities.
 */
export interface TextStreamIterator extends Streamable<TextStreamIterationResult> {
}
/**
 * Class for handling text streams using addon async iterator protocol.
 * Provides functionality to read text data from native stream implementations.
 * @internal
 */
export declare class TextStream extends AbstractStreamType implements TextStreamIterator {
    /**
     * @param stream
     * @param onDestroy
     */
    constructor(stream: any, onDestroy?: () => void);
    /**
     * Asynchronously reads the next item from the stream using addon async iterator protocol.
     *
     * @returns Promise resolving to a result object with text content or done flag
     * @throws InworldError if reading from the stream fails
     */
    next(): Promise<TextStreamIterationResult>;
    [Symbol.asyncIterator](): AsyncIterator<TextStreamIterationResult>;
    /**
     * Creates a new TextStream instance from a addon stream.
     *
     * @param stream - addon stream object or external reference
     * @returns A new TextStream instance
     * @internal
     */
    static fromExternal(stream: RuntimeInputStream<any>): TextStream;
    toExternal(): StreamingTaggedTypeBase<'TextStream', {
        stream: true;
    }, any>;
}
