import { RuntimeInputStream } from '../../internal/types';
export interface Streamable<IterationResultType> {
    /**
     * @internal
     */
    getStream?: () => RuntimeInputStream<any> | AsyncGenerator;
    /**
     * Asynchronously reads the next item from the stream.
     * @returns Promise resolving to a result object with IterationResultType data or done flag
     */
    next: () => Promise<IterationResultType>;
}
