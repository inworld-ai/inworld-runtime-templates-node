import { RuntimeInputStream, StreamingTaggedTypeBase } from '../../internal/types';
/**
 * Abstract base class for all API-level data types.
 */
export declare abstract class AbstractStreamType<StreamType extends string = string, DataType = {
    stream: true;
}, StreamDataType = any> {
    /**
     * @internal
     */
    protected stream: any;
    /**
     * @internal
     */
    protected onDestroy?: () => void;
    constructor(stream: any, onDestroy?: () => void);
    /**
     * @internal
     */
    abstract toExternal(): StreamingTaggedTypeBase<StreamType, DataType, StreamDataType>;
    static isStreamType(value: any): value is AbstractStreamType<any, any, any>;
    /**
     * @internal
     */
    getStream(): RuntimeInputStream<string>;
    /**
     * Gets metadata from the underlying stream.
     * Returns an empty object if metadata is not available.
     *
     * @returns {Record<string, any>} Metadata object from the stream
     */
    getMetadata(): Record<string, any>;
}
