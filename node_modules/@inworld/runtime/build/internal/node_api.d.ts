import { AddonEnvironment, JsConditionCallback, JsGraph, JsNodeCallback } from './types';
export declare const __resetInworldNodeAddonModule: () => void;
export declare const createGraph: (request: Record<string, any>) => Promise<JsGraph>;
/**
 * Register custom callback functions for nodes and edge conditions
 * @param callbacks - Object containing callback definitions
 * @param callbacks.nodes - Map of node type names to their callback functions
 * @param callbacks.conditions - Map of condition type names to their callback
 *     functions
 */
export declare const registerCallbacks: (callbacks: {
    nodes?: Record<string, JsNodeCallback>;
    conditions?: Record<string, JsConditionCallback>;
}) => void;
export declare const stopInworldRuntime: () => Promise<void>;
/**
 * Access to the addon environment for primitives2.
 * Provides access to all primitive namespaces (speech, llm, embeddings, etc.)
 */
export declare const getInworldAddonEnv: () => AddonEnvironment;
export type TelemetryNamespaceBinding = {
    configure(request: any): void;
    shutdown(): void;
    configureMetric(config: any): void;
};
export declare const telemetryNamespace: TelemetryNamespaceBinding;
export declare const SpanWrapper: any;
