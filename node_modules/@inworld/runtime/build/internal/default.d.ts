/**
 * Defaults for different Inworld Runtime services.
 * @internal
 */
export declare class Default {
    static telemetry: {
        /**
         * Default app version used for telemetry.
         * @internal
         */
        telemetryAppVersion: string;
        /**
         * Default app name used for telemetry.
         * @internal
         */
        telemetryAppName: string;
        /**
         * Default endpoint used for auth.
         *
         * You can override this by setting the INWORLD_ENV environment variable:
         *   DEV   -> api-engine.dev.inworld.ai:443
         *   STAGE -> api-engine.stage.inworld.ai:443
         *   PROD  -> api-engine.inworld.ai:443
         * Any other value or unset will use this default.
         * @internal
         */
        authEndpoint: string;
        flushTimeoutMs: number;
        shutdownTimeoutMs: number;
        logger: {
            sinkAbslLogs: boolean;
        };
        metrics: {
            exportInterval: number;
            exportTimeout: number;
            metricCacheSize: number;
        };
        tracer: {
            maxSpanCaptureQueueSize: number;
            enableSpanCapture: boolean;
            metricCacheSize: number;
            samplingRate: number;
        };
    };
    static embedder: {
        provider: string;
        modelName: string;
    };
    static llm: {
        provider: string;
        modelName: string;
    };
    static featureFlag: {
        evaluationMode: "EVALUATION_MODE_IN_PROCESS_ONLY";
        pollIntervalMs: number;
    };
}
