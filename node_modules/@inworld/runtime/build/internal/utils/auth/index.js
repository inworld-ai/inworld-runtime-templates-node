"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getAuthEndpointFromEnv = void 0;
const default_1 = require("../../default");
/**
 * @internal
 * Returns the auth endpoint based on the INWORLD_ENV environment variable.
 * Supported values:
 *   DEV   -> api-engine.dev.inworld.ai:443
 *   STAGE -> knivesout.stage.inworld.ai:443
 *   PROD  -> api-engine.inworld.ai:443
 * Falls back to DEFAULT_AUTH_ENDPOINT if not set or unrecognized.
 */
const getAuthEndpointFromEnv = () => {
    const env = (process.env.INWORLD_ENV || '').toUpperCase();
    switch (env) {
        case 'DEV':
            return 'api-engine.dev.inworld.ai:443';
        case 'STAGE':
            return 'knivesout.stage.inworld.ai:443';
        case 'PROD':
            return 'api-engine.inworld.ai:443';
        default:
            return default_1.Default.telemetry.authEndpoint;
    }
};
exports.getAuthEndpointFromEnv = getAuthEndpointFromEnv;
