export declare const SUPPORTED_PLATFORMS: {
    linux_x64: string;
    win32_x64: string;
    darwin_arm64: string;
};
export declare const SUPPORTED_NODE_ADDON_PLATFORMS: {
    linux_x64: string;
    win32_x64: string;
    darwin_arm64: string;
};
/**
 * Gets the platform and architecture of the current system.
 *
 * @returns Object containing the platform and architecture
 * @internal
 */
export declare const getPlatform: () => {
    platform: NodeJS.Platform;
    arch: string;
};
/**
 * Checks if the current platform is supported.
 *
 * @returns True if the platform is supported, false otherwise
 * @internal
 */
export declare const isSupportedPlatform: ({ platform, arch, nodeAddon, }: {
    platform: string;
    arch: string;
    nodeAddon?: boolean;
}) => boolean;
/**
 * Gets the path to the binary for the current platform.
 *
 * @returns The path to the binary
 * @internal
 */
export declare const getBinaryPath: (baseDir: string, nodeAddon?: boolean) => string;
