"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isStreamingTaggedType = isStreamingTaggedType;
exports.hasStreamingData = hasStreamingData;
/**
 * Type guard to check if a TaggedType has a stream field
 */
function isStreamingTaggedType(value) {
    return typeof value === 'object' && value !== null && '_stream' in value;
}
/**
 * Type guard to check if a TaggedType has streaming data
 */
function hasStreamingData(value) {
    return (typeof value === 'object' &&
        value !== null &&
        'data' in value &&
        typeof value.data === 'object' &&
        value.data !== null &&
        'stream' in value.data &&
        value.data.stream === true);
}
