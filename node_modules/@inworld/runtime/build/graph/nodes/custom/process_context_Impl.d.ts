import { DataStore, JsProcessContext } from '../../../internal/types';
import { LLMInterface, TextEmbedderInterface } from '../../../primitives/interfaces';
import { ExecutionConfig } from '../NodeExecutionConfig';
/**
 * Execution context.
 * Provides access to various information about the execution, to node execution config and to the {@link DataStore}.
 */
export interface ProcessContext {
    /**
     * Gets the execution configuration for the node.
     * @returns The execution configuration
     */
    getExecutionConfig<T = Record<string, any>>(): ExecutionConfig<T>;
    /**
     * Gets the {@link DataStore} for the execution.
     */
    getDatastore(): DataStore;
    /**
     * Gets the variant string from the context.
     * @returns The variant string
     */
    getVariant(): string;
    /**
     * Gets the execution ID from the context.
     * @returns The execution ID
     */
    getExecutionId(): string;
    getLLMInterface(componentId: string): LLMInterface;
    getEmbedderInterface(componentId: string): TextEmbedderInterface;
    nodeId: string;
    isCancelled: boolean;
}
/**
 * @internal
 * Provides access to execution context information for custom nodes.
 */
export declare class ProcessContextImpl implements ProcessContext {
    private external;
    private datastore;
    private executionConfig?;
    private llmInterface?;
    private textEmbedderInterface?;
    constructor(external: JsProcessContext, executionConfig?: ExecutionConfig);
    /**
     * Gets the datastore for the execution.
     * @returns The datastore
     */
    getDatastore(): DataStore;
    /**
     * Gets the execution ID from the context.
     * @returns The execution ID
     */
    getExecutionId(): string;
    /**
     * Gets the variant string from the context.
     * @returns The variant string
     */
    getVariant(): string;
    /**
     * Checks if the execution has been cancelled.
     * @returns True if cancelled, false otherwise
     */
    get isCancelled(): boolean;
    /**
     * Gets the configuration helper from the context.
     * @returns The configuration object
     */
    getExecutionConfig<PropertiesType>(): ExecutionConfig<PropertiesType>;
    get nodeId(): string;
    getLLMInterface(componentId: string): LLMInterface;
    getEmbedderInterface(componentId: string): TextEmbedderInterface;
}
