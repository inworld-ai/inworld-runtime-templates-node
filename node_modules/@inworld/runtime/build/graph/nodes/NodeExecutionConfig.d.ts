/**
 * Represents the properties for the execution config.
 */
export interface ExecutionConfig<T = Record<string, any>> {
    /**
     * Whether to report to the client.
     */
    reportToClient?: boolean;
    properties: T;
}
