import { Component as GraphConfigComponent } from '../graph_config_schema';
import { AbstractComponent, AbstractComponentProps } from './abstract_component';
interface SafetyCheckerComponentProps extends AbstractComponentProps {
    /** Path to model weights for logistic regression classifiers */
    modelWeightsPath: string;
    /** Text embedder component ID to use for semantic analysis */
    embedderComponentId: string;
}
export declare class SafetyCheckerComponent extends AbstractComponent {
    private modelWeightsPath;
    private embedderComponentId;
    /**
     * Creates a new SafetyCheckerComponent instance.
     *
     * @param props - Configuration for the safety checker component.
     */
    constructor(props: SafetyCheckerComponentProps);
    /**
     * Converts the safety checker component to a graph configuration component.
     *
     * @returns The configured safety checker component for graph execution.
     */
    protected toGraphConfigComponent(): GraphConfigComponent;
}
export {};
