import { Component as GraphConfigComponent } from '../graph_config_schema';
import { AbstractComponent, type AbstractComponentProps } from './abstract_component';
interface DeviceInfo {
    type?: 'CPU' | 'CUDA' | 'Metal' | 'ROCm';
    index?: number;
    info?: {
        name?: string;
        timestamp?: number;
        freeMemoryBytes?: number;
        totalMemoryBytes?: number;
        [key: string]: unknown;
    };
    [key: string]: unknown;
}
/**
 * Configuration options for {@link LocalTurnDetectorComponent}.
 */
export interface LocalTurnDetectorComponentProps extends AbstractComponentProps {
    /**
     * Path to the turn detector model file.
     */
    modelPath: string;
    /**
     * Optional device selection to run inference on.
     */
    device?: DeviceInfo;
    /**
     * Threshold used to detect speaker turn transitions.
     * @default 0.5
     */
    threshold?: number;
}
/**
 * Local turn detector component that wraps `LocalTurnDetectorConfig`.
 *
 * @example
 * ```typescript
 * const turnDetector = new LocalTurnDetectorComponent({
 *   id: 'local_turn_detector',
 *   modelPath: '{{TURN_DETECTOR_MODEL_PATH}}',
 *   threshold: 0.55,
 * });
 * ```
 */
export declare class LocalTurnDetectorComponent extends AbstractComponent {
    private readonly modelPath;
    private readonly device?;
    private readonly threshold?;
    /**
     * Creates a new {@link LocalTurnDetectorComponent}.
     *
     * @param {LocalTurnDetectorComponentProps} props - Component configuration.
     */
    constructor(props: LocalTurnDetectorComponentProps);
    protected toGraphConfigComponent(): GraphConfigComponent;
}
export {};
