import { Camelize } from '../constants';
import { Component as GraphConfigComponent, GroqServiceConfig, InworldServiceConfig, SpeechRecognitionConfig } from '../graph_config_schema';
import { AbstractComponent, AbstractComponentProps } from './abstract_component';
/**
 * Configuration options for `RemoteSTTComponent` creation.
 *
 * @remarks `sttConfig` accepts camelCase properties.
 */
interface RemoteSTTComponentProps extends AbstractComponentProps {
    /** STT configuration object. */
    sttConfig: Camelize<SpeechRecognitionConfig>;
    /** STT service provider (defaults to 'inworld'). */
    service?: 'inworld' | 'groq';
    /** Model ID to use (defaults to 'groq/whisper-large-v3'). */
    modelId?: string;
    /**
     * Optional overrides for the generated service-specific configuration.
     * Accepts camelCase properties matching {@link InworldServiceConfig} or {@link GroqServiceConfig}.
     */
    configOverrides?: Partial<Camelize<InworldServiceConfig>> | Partial<Camelize<GroqServiceConfig>>;
}
/**
 * Remote STT (Speech-to-Text) component for audio transcription services.
 * Provides access to external speech recognition services to convert audio
 * input into text transcriptions.
 *
 * @remarks
 * API key injection is handled by the graph builder at assembly time. You do not
 * need to include secrets in this component definition.
 *
 * @example
 * ```typescript
 * const sttComponent = new RemoteSTTComponent({
 *   id: 'my-stt-component',
 *   sttConfig: {
 *     languageCode: 'en-US'
 *   },
 *   service: 'inworld',
 *   modelId: 'groq/whisper-large-v3'
 * });
 * ```
 */
export declare class RemoteSTTComponent extends AbstractComponent {
    private sttConfig;
    private service;
    private modelId;
    private configOverrides?;
    /**
     * Creates a new RemoteSTTComponent instance.
     *
     * @param props - Configuration for the remote STT component.
     */
    constructor(props: RemoteSTTComponentProps);
    /**
     * Converts the remote STT component to a graph configuration component.
     *
     * @returns The configured remote STT component for graph execution.
     */
    protected toGraphConfigComponent(): GraphConfigComponent;
    private buildServiceConfig;
    private getDefaultApiKeyPlaceholder;
}
export {};
