import { Camelize } from '../constants';
import { Component as GraphConfigComponent, StreamSpeechRecognitionConfig2 } from '../graph_config_schema';
import { AbstractComponent, type AbstractComponentProps } from './abstract_component';
/**
 * Configuration options for {@link RemoteStreamingSTTComponent}.
 */
export interface RemoteStreamingSTTComponentProps extends AbstractComponentProps {
    /**
     * Optional default streaming STT configuration.
     */
    defaultConfig?: Camelize<StreamSpeechRecognitionConfig2>;
    /**
     * Optional model identifier for the remote streaming service.
     */
    modelId?: string;
    /**
     * Optional endpoint override for custom deployments.
     */
    endpointOverride?: string;
    /**
     * Optional timeout (in seconds) for streaming requests.
     */
    defaultTimeout?: number | string;
    /**
     * Optional API key template; when omitted, the graph builder injects `{{INWORLD_API_KEY}}`.
     */
    apiKey?: string;
}
/**
 * Remote streaming STT component for bidirectional transcription services.
 *
 * @example
 * ```typescript
 * const streamingStt = new RemoteStreamingSTTComponent({
 *   id: 'assembly_streaming_stt',
 *   modelId: 'assemblyai/universal-streaming-multilingual',
 *   defaultConfig: {
 *     silenceThresholdMs: 1500,
 *   },
 * });
 * ```
 */
export declare class RemoteStreamingSTTComponent extends AbstractComponent {
    private readonly defaultConfig?;
    private readonly modelId?;
    private readonly endpointOverride?;
    private readonly defaultTimeout?;
    private readonly apiKey?;
    constructor(props: RemoteStreamingSTTComponentProps);
    protected toGraphConfigComponent(): GraphConfigComponent;
}
