import { Component as GraphConfigComponent } from '../graph_config_schema';
import { AbstractComponent, type AbstractComponentProps } from './abstract_component';
interface DeviceInfo {
    type?: 'CPU' | 'CUDA' | 'Metal' | 'ROCm';
    index?: number;
    info?: {
        name?: string;
        timestamp?: number;
        freeMemoryBytes?: number;
        totalMemoryBytes?: number;
        [key: string]: unknown;
    };
    [key: string]: unknown;
}
/**
 * Configuration options for {@link LocalVADComponent}.
 */
export interface LocalVADComponentProps extends AbstractComponentProps {
    /**
     * Path to the VAD model file. Typically a substitution such as `{{VAD_MODEL_PATH}}`.
     */
    modelPath: string;
    /**
     * Optional device selection for running inference.
     * Defaults to CPU with placeholder metadata.
     */
    device?: DeviceInfo;
    /**
     * Speech detection threshold to use when no custom config is provided.
     * @default 0.5
     */
    speechThreshold?: number;
}
/**
 * Local VAD component that wraps `LocalVADConfig`.
 *
 * @remarks
 * Mirrors the realtime-agent blueprint defaults while allowing callers to
 * override the model path, device information, or speech threshold.
 *
 * @example
 * ```typescript
 * const vad = new LocalVADComponent({
 *   id: 'local_vad',
 *   modelPath: '{{VAD_MODEL_PATH}}',
 *   speechThreshold: 0.6,
 * });
 * ```
 */
export declare class LocalVADComponent extends AbstractComponent {
    private readonly modelPath;
    private readonly device?;
    private readonly speechThreshold?;
    /**
     * Creates a new {@link LocalVADComponent}.
     *
     * @param {LocalVADComponentProps} props - Component configuration.
     */
    constructor(props: LocalVADComponentProps);
    protected toGraphConfigComponent(): GraphConfigComponent;
}
export {};
