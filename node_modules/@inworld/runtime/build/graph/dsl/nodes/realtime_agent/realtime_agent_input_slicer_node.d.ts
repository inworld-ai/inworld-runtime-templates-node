import { Node as GraphConfigNode } from '../../graph_config_schema';
import { AbstractNode, type AbstractNodeProps } from '../abstract_node';
/**
 * Configuration for {@link RealtimeAgentInputSlicerNode}.
 */
export interface RealtimeAgentInputSlicerNodeProps extends AbstractNodeProps {
    /**
     * ID of the VAD component to use.
     *
     * In the C++ blueprint this is typically `local_vad`.
     */
    vadComponentId: string;
    /**
     * Optional ID of the turn detector component to use.
     *
     * In the C++ blueprint this is typically `local_turn_detector`.
     */
    turnDetectorComponentId?: string;
    /**
     * Maximum duration per turn in milliseconds.
     * @default 30000
     */
    maxSpeechDurationPerTurnMs?: number;
    /**
     * Silence duration in milliseconds to finish a turn.
     * @default 200
     */
    maxSilenceToFinishTurnMs?: number;
    /**
     * Audio sample rate in Hz.
     * @default 16000
     */
    sampleRate?: number;
}
/**
 * Built-in realtime agent node that slices an incoming multimodal stream
 * (audio or text) into interaction turns and detects interruptions.
 *
 * @remarks
 * This node is designed to be used with a self-loop edge to keep consuming
 * streaming input until the stream completes.
 *
 * @input {GraphTypes.MultimodalContentStream} {@link GraphTypes.MultimodalContentStream} - Incoming multimodal stream (audio or text chunks)
 * @output {GraphTypes.Custom<any>} {@link GraphTypes.Custom} - Internal slicer state (not typically consumed directly; used for routing)
 */
export declare class RealtimeAgentInputSlicerNode extends AbstractNode {
    private readonly executionConfig;
    /**
     * Creates a new {@link RealtimeAgentInputSlicerNode}.
     *
     * @param {RealtimeAgentInputSlicerNodeProps} props - Node configuration.
     */
    constructor(props: RealtimeAgentInputSlicerNodeProps);
    protected toGraphConfigNode(): GraphConfigNode;
}
