import { Node as GraphConfigNode } from '../../graph_config_schema';
import { AbstractNode, type AbstractNodeProps } from '../abstract_node';
/**
 * Built-in realtime agent node that prepares a JSON object with variables for
 * rendering an LLM prompt template (e.g., user query + conversation history).
 *
 * @input {String} {@link String} - User query text
 * @output {GraphTypes.Custom<any>} {@link GraphTypes.Custom} - JSON prompt variables
 */
export declare class RealtimeAgentPromptVariablesNode extends AbstractNode {
    /**
     * Creates a new {@link RealtimeAgentPromptVariablesNode}.
     *
     * @param {AbstractNodeProps} [props] - Optional node configuration.
     */
    constructor(props?: AbstractNodeProps);
    protected toGraphConfigNode(): GraphConfigNode;
}
