import { RuntimeAllowedType } from '../../common/data_types/type_mapping';
import { WrappedType } from '../../internal/types';
/**
 * @internal
 * Converts CustomNodeOutputTypes to addon TaggedType format.
 * Handles both regular data types and streaming types, creating proper TaggedType
 * objects with _stream fields for stream instances.
 *
 * @param input - The output from a custom node to convert
 * @returns TaggedType formatted for addon consumption
 */
export declare function toExternal(input: RuntimeAllowedType): WrappedType;
