# Inworld Runtime API Key
# Get your API key from: https://studio.inworld.ai
INWORLD_API_KEY=your_api_key_here

# Optional: Override default configurations
# DEFAULT_LLM_MODEL_NAME=gpt-4o-mini
# DEFAULT_LLM_PROVIDER=openai
# DEFAULT_TTS_MODEL_ID=inworld-tts-1-max
# DEFAULT_VOICE_ID=Ashley
# SAMPLE_RATE=48000

# Benchmark Configuration Examples:
# Fast TTS Load Test:
# yarn bench:tts "Hello world" --threads=5 --requests_per_thread=10 --config=fast

# Medium TTS Load Test:
# yarn bench:tts "Hello world" --threads=3 --requests_per_thread=5 --config=medium

# Two-Node Pipeline Test:
# yarn bench:tts-two-node "Hello world" --threads=2 --requests_per_thread=3 --config=fast

# Custom Reverse Load Test:
# yarn bench:custom-reverse "Hello world" --threads=10 --requests_per_thread=20 --config=cpu-intensive

# Infinite LLM Chat (with memory monitoring):
# yarn bench:llm-infinite "Tell me a fact" --sleepSeconds=10 --modelName=gpt-4o-mini

# Network Load Test (continuous execution):
# yarn bench:net
